//
   Created by jay on 4/30/17.
doctype html
html
    head
        meta(charset='utf-8')
        title Join | Chat App
        meta(name='viewport' content='width=device-width, initial-scale=1, user-scalable=no')
        link(rel='stylesheet' href='css/main.css')
    body.centered-form
        div.centered-form__form
            form(action='/chat')
                div.form-field
                    h3 Join a Chat
                div.form-field
                    label Display name
                    input(type="text" name="name" autofocus)
                div.form-field
                    label Room name
                    input(type="text" name="room")
                //div.form-field
                //    label Room list
                //    select
                //        if roomsList != undefined && roomsList.length > 0
                //            each room in roomsList
                //                option= room
                div.form-field
                    button Join
    footer
